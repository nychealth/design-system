/**
 * @nychealth/design-system/tokens — JS entrypoint.
 *
 * CSS tokens (tokens.css, theme.css) are imported directly by path from a
 * consuming app's CSS entry point — see theme.css for usage. This file only
 * re-exports the JS-side tokens (chart colors, health direction, spacing).
 */
export * from "./tokens.js";
